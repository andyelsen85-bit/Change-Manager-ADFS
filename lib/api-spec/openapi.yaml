openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: ITIL v4 Change Management API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: users
  - name: roles
  - name: changes
  - name: templates
  - name: planning
  - name: testing
  - name: approvals
  - name: cab
  - name: comments
  - name: dashboard
  - name: settings
  - name: audit
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HealthStatus" }

  # ---------- AUTH ----------
  /auth/login:
    post:
      operationId: login
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginBody" }
      responses:
        "200":
          description: Logged in
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionUser" }
        "401":
          description: Invalid credentials
          content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
  /auth/setup-status:
    get:
      operationId: getSetupStatus
      tags: [auth]
      description: |
        First-time setup probe. Returns `{ needsSetup: true }` only when the
        seeded `admin` row exists with no password — i.e. the operator has
        not yet completed first-time setup. Unauthenticated.
      responses:
        "200":
          description: Setup status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SetupStatus" }
  /auth/setup:
    post:
      operationId: completeSetup
      tags: [auth]
      description: |
        Claim the seeded `admin` account by setting its password. Only valid
        while `getSetupStatus` reports `needsSetup: true`. On success the
        response also sets the session and CSRF cookies so the operator is
        signed in immediately. Returns 409 once setup has already been done.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SetupBody" }
      responses:
        "200":
          description: Setup complete; session established
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionUser" }
        "400":
          description: Bad request
          content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
        "409":
          description: Setup already completed
          content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      responses:
        "204": { description: Logged out }
  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionUser" }
        "401":
          description: Not authenticated
          content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } }
  /auth/change-password:
    post:
      operationId: changePassword
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChangePasswordBody" }
      responses:
        "204": { description: OK }
        "400": { description: Bad request, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

  # ---------- USERS ----------
  /users:
    get:
      operationId: listUsers
      tags: [users]
      parameters:
        - { in: query, name: search, schema: { type: string } }
        - { in: query, name: role, schema: { type: string } }
        - in: query
          name: primary
          description: When combined with `role`, restrict to non-deputy assignments.
          schema: { type: string, enum: ["1", "true"] }
      responses:
        "200":
          description: Users
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/User" }
    post:
      operationId: createUser
      tags: [users]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateUserBody" } } }
      responses:
        "201":
          description: Created
          content: { application/json: { schema: { $ref: "#/components/schemas/User" } } }
  /users/{id}:
    get:
      operationId: getUser
      tags: [users]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: User
          content: { application/json: { schema: { $ref: "#/components/schemas/User" } } }
    patch:
      operationId: updateUser
      tags: [users]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateUserBody" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/User" } } }
    delete:
      operationId: deleteUser
      tags: [users]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "204": { description: Deleted }
  /users/{id}/notification-preferences:
    get:
      operationId: getNotificationPreferences
      tags: [users]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Prefs
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/NotificationPreference" }
    put:
      operationId: updateNotificationPreferences
      tags: [users]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items: { $ref: "#/components/schemas/NotificationPreference" }
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/NotificationPreference" }

  # ---------- ROLES ----------
  /roles:
    get:
      operationId: listRoles
      tags: [roles]
      responses:
        "200":
          description: Roles
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Role" }
  /role-assignments:
    get:
      operationId: listRoleAssignments
      tags: [roles]
      responses:
        "200":
          description: Assignments
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/RoleAssignment" }
    post:
      operationId: createRoleAssignment
      tags: [roles]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateRoleAssignmentBody" } } }
      responses:
        "201":
          description: Created
          content: { application/json: { schema: { $ref: "#/components/schemas/RoleAssignment" } } }
  /role-assignments/{id}:
    delete:
      operationId: deleteRoleAssignment
      tags: [roles]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "204": { description: Deleted }

  # ---------- CHANGES ----------
  /changes:
    get:
      operationId: listChanges
      tags: [changes]
      parameters:
        - { in: query, name: status, schema: { type: string } }
        - { in: query, name: track, schema: { type: string } }
        - { in: query, name: ownerId, schema: { type: integer } }
        - { in: query, name: search, schema: { type: string } }
      responses:
        "200":
          description: Changes
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ChangeRequest" }
    post:
      operationId: createChange
      tags: [changes]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateChangeBody" } } }
      responses:
        "201":
          description: Created
          content: { application/json: { schema: { $ref: "#/components/schemas/ChangeRequest" } } }
  /changes/{id}:
    get:
      operationId: getChange
      tags: [changes]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Change
          content: { application/json: { schema: { $ref: "#/components/schemas/ChangeDetail" } } }
    patch:
      operationId: updateChange
      tags: [changes]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateChangeBody" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/ChangeRequest" } } }
    delete:
      operationId: deleteChange
      tags: [changes]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "204": { description: Deleted }
  /changes/{id}/track:
    post:
      operationId: changeTrack
      tags: [changes]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/ChangeTrackBody" } } }
      responses:
        "200":
          description: Track switched; change reset to draft, approvals cleared
          content: { application/json: { schema: { $ref: "#/components/schemas/ChangeRequest" } } }
        "400": { description: Invalid track, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
        "409": { description: Change is past Draft/Submitted, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
  /changes/{id}/transition:
    post:
      operationId: transitionChange
      tags: [changes]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/TransitionBody" } } }
      responses:
        "200":
          description: Transitioned
          content: { application/json: { schema: { $ref: "#/components/schemas/ChangeRequest" } } }
        "400": { description: Invalid transition, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
  /changes/{id}/revert:
    post:
      operationId: revertChange
      tags: [changes]
      summary: Walk a change back to an earlier status (Change Manager / Admin only).
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/RevertBody" } } }
      responses:
        "200":
          description: Reverted
          content: { application/json: { schema: { $ref: "#/components/schemas/ChangeRequest" } } }
        "400": { description: Invalid revert target, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
        "403": { description: Forbidden, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

  # ---------- TEMPLATES ----------
  /templates:
    get:
      operationId: listTemplates
      tags: [templates]
      responses:
        "200":
          description: Templates
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/StandardTemplate" }
    post:
      operationId: createTemplate
      tags: [templates]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateTemplateBody" } } }
      responses:
        "201":
          description: Created
          content: { application/json: { schema: { $ref: "#/components/schemas/StandardTemplate" } } }
  /templates/{id}:
    get:
      operationId: getTemplate
      tags: [templates]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Template
          content: { application/json: { schema: { $ref: "#/components/schemas/StandardTemplate" } } }
    patch:
      operationId: updateTemplate
      tags: [templates]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateTemplateBody" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/StandardTemplate" } } }
    delete:
      operationId: deleteTemplate
      tags: [templates]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "204": { description: Deleted }

  # ---------- PLANNING ----------
  /changes/{id}/planning:
    get:
      operationId: getPlanning
      tags: [planning]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Planning
          content: { application/json: { schema: { $ref: "#/components/schemas/PlanningRecord" } } }
    put:
      operationId: upsertPlanning
      tags: [planning]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpsertPlanningBody" } } }
      responses:
        "200":
          description: Saved
          content: { application/json: { schema: { $ref: "#/components/schemas/PlanningRecord" } } }

  # ---------- TESTING / PIR ----------
  /changes/{id}/testing:
    get:
      operationId: getTesting
      tags: [testing]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Testing
          content: { application/json: { schema: { $ref: "#/components/schemas/TestRecord" } } }
    put:
      operationId: upsertTesting
      tags: [testing]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpsertTestingBody" } } }
      responses:
        "200":
          description: Saved
          content: { application/json: { schema: { $ref: "#/components/schemas/TestRecord" } } }
  /changes/{id}/pir:
    get:
      operationId: getPir
      tags: [testing]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: PIR
          content: { application/json: { schema: { $ref: "#/components/schemas/PirRecord" } } }
    put:
      operationId: upsertPir
      tags: [testing]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpsertPirBody" } } }
      responses:
        "200":
          description: Saved
          content: { application/json: { schema: { $ref: "#/components/schemas/PirRecord" } } }

  # ---------- APPROVALS ----------
  /changes/{id}/approvals:
    get:
      operationId: listApprovals
      tags: [approvals]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Approvals
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Approval" }
  /approvals/{id}/vote:
    post:
      operationId: voteApproval
      tags: [approvals]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/VoteBody" } } }
      responses:
        "200":
          description: Voted
          content: { application/json: { schema: { $ref: "#/components/schemas/Approval" } } }

  # ---------- CAB ----------
  /cab-meetings:
    get:
      operationId: listCabMeetings
      tags: [cab]
      parameters:
        - { in: query, name: from, schema: { type: string, format: date-time } }
        - { in: query, name: to, schema: { type: string, format: date-time } }
        - { in: query, name: kind, schema: { type: string } }
      responses:
        "200":
          description: Meetings
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CabMeeting" }
    post:
      operationId: createCabMeeting
      tags: [cab]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateCabMeetingBody" } } }
      responses:
        "201":
          description: Created
          content: { application/json: { schema: { $ref: "#/components/schemas/CabMeeting" } } }
  /cab-meetings/{id}:
    get:
      operationId: getCabMeeting
      tags: [cab]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Meeting
          content: { application/json: { schema: { $ref: "#/components/schemas/CabMeetingDetail" } } }
    patch:
      operationId: updateCabMeeting
      tags: [cab]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateCabMeetingBody" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/CabMeeting" } } }
    delete:
      operationId: deleteCabMeeting
      tags: [cab]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "204": { description: Deleted }
  /cab-meetings/{id}/ics:
    get:
      operationId: getCabMeetingIcs
      tags: [cab]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: ICS calendar invite
          content:
            text/calendar:
              schema: { type: string }
  /cab-meetings/{id}/send-agenda:
    post:
      operationId: sendCabAgenda
      tags: [cab]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Agenda sent — counts of sent/skipped/errored deliveries.
          content:
            application/json:
              schema:
                type: object
                required: [sent, skipped, errors]
                properties:
                  sent:    { type: integer, description: "Members who received the agenda email." }
                  skipped: { type: integer, description: "Members whose notification preference opted out, or for whom no SMTP was configured." }
                  errors:  { type: integer, description: "Members for whom the SMTP send failed." }

  # ---------- COMMENTS ----------
  /changes/{id}/comments:
    get:
      operationId: listComments
      tags: [comments]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Comments
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Comment" }
    post:
      operationId: createComment
      tags: [comments]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateCommentBody" } } }
      responses:
        "201":
          description: Created
          content: { application/json: { schema: { $ref: "#/components/schemas/Comment" } } }

  # ---------- DASHBOARD ----------
  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      responses:
        "200":
          description: Summary
          content: { application/json: { schema: { $ref: "#/components/schemas/DashboardSummary" } } }
  /dashboard/activity:
    get:
      operationId: getDashboardActivity
      tags: [dashboard]
      responses:
        "200":
          description: Activity
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ActivityItem" }
  /dashboard/upcoming-cab:
    get:
      operationId: getUpcomingCab
      tags: [dashboard]
      responses:
        "200":
          description: Upcoming
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CabMeeting" }
  /dashboard/my-tasks:
    get:
      operationId: getMyTasks
      tags: [dashboard]
      responses:
        "200":
          description: Tasks
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TaskItem" }

  # ---------- SETTINGS ----------
  /settings/smtp:
    get:
      operationId: getSmtpSettings
      tags: [settings]
      responses:
        "200":
          description: SMTP
          content: { application/json: { schema: { $ref: "#/components/schemas/SmtpSettings" } } }
    put:
      operationId: updateSmtpSettings
      tags: [settings]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/SmtpSettings" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/SmtpSettings" } } }
  /settings/smtp/test:
    post:
      operationId: testSmtp
      tags: [settings]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/TestEmailBody" } } }
      responses:
        "200":
          description: Result
          content: { application/json: { schema: { $ref: "#/components/schemas/TestResult" } } }
  /settings/ldap:
    get:
      operationId: getLdapSettings
      tags: [settings]
      responses:
        "200":
          description: LDAP
          content: { application/json: { schema: { $ref: "#/components/schemas/LdapSettings" } } }
    put:
      operationId: updateLdapSettings
      tags: [settings]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/LdapSettings" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/LdapSettings" } } }
  /settings/ldap/test:
    post:
      operationId: testLdap
      tags: [settings]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/TestLdapBody" } } }
      responses:
        "200":
          description: Diagnostic result of the bind attempt — includes which stage failed and the underlying LDAP error code so admins can fix configuration without reading server logs.
          content: { application/json: { schema: { $ref: "#/components/schemas/LdapTestResult" } } }
  /settings/ssl:
    get:
      operationId: getSslSettings
      tags: [settings]
      responses:
        "200":
          description: SSL
          content: { application/json: { schema: { $ref: "#/components/schemas/SslSettings" } } }
    put:
      operationId: updateSslSettings
      tags: [settings]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSslBody" } } }
      responses:
        "200":
          description: Updated
          content: { application/json: { schema: { $ref: "#/components/schemas/SslSettings" } } }

  # ---------- AUDIT ----------
  /audit-log:
    get:
      operationId: listAuditLog
      tags: [audit]
      parameters:
        - { in: query, name: actorId, schema: { type: integer } }
        - { in: query, name: action, schema: { type: string } }
        - { in: query, name: entityType, schema: { type: string } }
        - { in: query, name: entityId, schema: { type: integer } }
        - { in: query, name: from, schema: { type: string, format: date-time } }
        - { in: query, name: to, schema: { type: string, format: date-time } }
        - { in: query, name: limit, schema: { type: integer } }
        - { in: query, name: offset, schema: { type: integer } }
      responses:
        "200":
          description: Entries
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AuditEntry" }
        "403": { description: Forbidden, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
  /audit-log/export:
    get:
      operationId: exportAuditLog
      tags: [audit]
      parameters:
        - { in: query, name: actorId, schema: { type: integer } }
        - { in: query, name: action, schema: { type: string } }
        - { in: query, name: entityType, schema: { type: string } }
        - { in: query, name: from, schema: { type: string, format: date-time } }
        - { in: query, name: to, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: CSV
          content:
            text/csv:
              schema: { type: string }
        "403": { description: Forbidden, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status: { type: string }
      required: [status]

    Error:
      type: object
      properties:
        error: { type: string }
      required: [error]

    LoginBody:
      type: object
      properties:
        username: { type: string }
        password: { type: string }
      required: [username, password]

    ChangePasswordBody:
      type: object
      properties:
        currentPassword: { type: string }
        newPassword: { type: string }
      required: [currentPassword, newPassword]

    SetupStatus:
      type: object
      properties:
        needsSetup: { type: boolean }
      required: [needsSetup]

    SetupBody:
      type: object
      properties:
        password: { type: string, minLength: 8 }
      required: [password]

    SessionUser:
      type: object
      properties:
        id: { type: integer }
        username: { type: string }
        email: { type: string }
        fullName: { type: string }
        source: { type: string, enum: [local, ldap] }
        roles:
          type: array
          items: { type: string }
        isAdmin: { type: boolean }
        mustChangePassword:
          type: boolean
          description: True when the user must rotate their password before using the rest of the app (e.g. seeded admin on first login).
      required: [id, username, email, fullName, source, roles, isAdmin, mustChangePassword]

    User:
      type: object
      properties:
        id: { type: integer }
        username: { type: string }
        email: { type: string }
        fullName: { type: string }
        source: { type: string, enum: [local, ldap] }
        isActive: { type: boolean }
        isAdmin: { type: boolean }
        deputyUserId: { type: ["integer", "null"] }
        deputyUserName: { type: ["string", "null"] }
        roles:
          type: array
          items: { type: string }
        createdAt: { type: string, format: date-time }
      required: [id, username, email, fullName, source, isActive, isAdmin, roles, createdAt]

    CreateUserBody:
      type: object
      properties:
        username: { type: string }
        email: { type: string }
        fullName: { type: string }
        password: { type: ["string", "null"] }
        source: { type: string, enum: [local, ldap] }
        isAdmin: { type: boolean }
        deputyUserId: { type: ["integer", "null"] }
        roles:
          type: array
          items: { type: string }
      required: [username, email, fullName, source]

    UpdateUserBody:
      type: object
      properties:
        email: { type: string }
        fullName: { type: string }
        isActive: { type: boolean }
        isAdmin: { type: boolean }
        password: { type: ["string", "null"] }
        deputyUserId: { type: ["integer", "null"] }
        roles:
          type: array
          items: { type: string }

    NotificationPreference:
      type: object
      properties:
        eventKey: { type: string }
        emailEnabled: { type: boolean }
        inAppEnabled: { type: boolean }
      required: [eventKey, emailEnabled, inAppEnabled]

    Role:
      type: object
      properties:
        key: { type: string }
        name: { type: string }
        description: { type: string }
        allowsDeputy: { type: boolean }
      required: [key, name, description, allowsDeputy]

    RoleAssignment:
      type: object
      properties:
        id: { type: integer }
        roleKey: { type: string }
        userId: { type: integer }
        userName: { type: string }
        isDeputy: { type: boolean }
        primaryAssignmentId: { type: ["integer", "null"] }
      required: [id, roleKey, userId, userName, isDeputy]

    CreateRoleAssignmentBody:
      type: object
      properties:
        roleKey: { type: string }
        userId: { type: integer }
        isDeputy: { type: boolean }
        primaryAssignmentId: { type: ["integer", "null"] }
      required: [roleKey, userId, isDeputy]

    ChangeRequest:
      type: object
      properties:
        id: { type: integer }
        ref: { type: string }
        title: { type: string }
        description: { type: string }
        track: { type: string, enum: [normal, standard, emergency] }
        status: { type: string }
        risk: { type: string, enum: [low, medium, high] }
        impact: { type: string, enum: [low, medium, high] }
        priority: { type: string, enum: [low, medium, high, critical] }
        category: { type: string }
        ownerId: { type: integer }
        ownerName: { type: string }
        assigneeId: { type: ["integer", "null"] }
        assigneeName: { type: ["string", "null"] }
        templateId: { type: ["integer", "null"] }
        templateName: { type: ["string", "null"] }
        plannedStart: { type: ["string", "null"], format: date-time }
        plannedEnd: { type: ["string", "null"], format: date-time }
        actualStart: { type: ["string", "null"], format: date-time }
        actualEnd: { type: ["string", "null"], format: date-time }
        cabMeetingId: { type: ["integer", "null"] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      required: [id, ref, title, description, track, status, risk, impact, priority, category, ownerId, ownerName, createdAt, updatedAt]

    ChangeDetail:
      allOf:
        - $ref: "#/components/schemas/ChangeRequest"
        - type: object
          properties:
            planning: { $ref: "#/components/schemas/PlanningRecord" }
            testing: { $ref: "#/components/schemas/TestRecord" }
            pir: { $ref: "#/components/schemas/PirRecord" }
            approvals:
              type: array
              items: { $ref: "#/components/schemas/Approval" }
            comments:
              type: array
              items: { $ref: "#/components/schemas/Comment" }
            trackChange:
              type: ["object", "null"]
              properties:
                from: { type: ["string", "null"] }
                to: { type: ["string", "null"] }
                at: { type: string, format: date-time }
                by: { type: string }

    CreateChangeBody:
      type: object
      properties:
        title: { type: string }
        description: { type: string }
        track: { type: string, enum: [normal, standard, emergency] }
        risk: { type: string, enum: [low, medium, high] }
        impact: { type: string, enum: [low, medium, high] }
        priority: { type: string, enum: [low, medium, high, critical] }
        category: { type: string }
        templateId: { type: ["integer", "null"] }
        assigneeId: { type: ["integer", "null"] }
        plannedStart: { type: ["string", "null"], format: date-time }
        plannedEnd: { type: ["string", "null"], format: date-time }
      required: [title, description, track, risk, impact, priority, category]

    UpdateChangeBody:
      type: object
      properties:
        title: { type: string }
        description: { type: string }
        risk: { type: string, enum: [low, medium, high] }
        impact: { type: string, enum: [low, medium, high] }
        priority: { type: string, enum: [low, medium, high, critical] }
        category: { type: string }
        assigneeId: { type: ["integer", "null"] }
        plannedStart: { type: ["string", "null"], format: date-time }
        plannedEnd: { type: ["string", "null"], format: date-time }
        cabMeetingId: { type: ["integer", "null"] }

    TransitionBody:
      type: object
      properties:
        toStatus: { type: string }
        note: { type: ["string", "null"] }
      required: [toStatus]

    ChangeTrackBody:
      type: object
      properties:
        track: { type: string, enum: [normal, standard, emergency] }
      required: [track]

    RevertBody:
      type: object
      properties:
        toStatus: { type: string, description: "Target earlier status to revert to." }
        reason: { type: string, minLength: 5, description: "Required justification, captured in the audit log." }
      required: [toStatus, reason]

    StandardTemplate:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        description: { type: string }
        category: { type: string }
        risk: { type: string, enum: [low, medium, high] }
        impact: { type: string, enum: [low, medium, high] }
        autoApprove: { type: boolean }
        bypassCab: { type: boolean }
        defaultPriority: { type: string, enum: [low, medium, high, critical] }
        prefilledPlanning: { type: ["string", "null"] }
        prefilledTestPlan: { type: ["string", "null"] }
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
      required: [id, name, description, category, risk, impact, autoApprove, bypassCab, defaultPriority, isActive, createdAt]

    CreateTemplateBody:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        category: { type: string }
        risk: { type: string, enum: [low, medium, high] }
        impact: { type: string, enum: [low, medium, high] }
        autoApprove: { type: boolean }
        bypassCab: { type: boolean }
        defaultPriority: { type: string, enum: [low, medium, high, critical] }
        prefilledPlanning: { type: ["string", "null"] }
        prefilledTestPlan: { type: ["string", "null"] }
      required: [name, description, category, risk, impact, autoApprove, bypassCab, defaultPriority]

    UpdateTemplateBody:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        category: { type: string }
        risk: { type: string, enum: [low, medium, high] }
        impact: { type: string, enum: [low, medium, high] }
        autoApprove: { type: boolean }
        bypassCab: { type: boolean }
        defaultPriority: { type: string, enum: [low, medium, high, critical] }
        prefilledPlanning: { type: ["string", "null"] }
        prefilledTestPlan: { type: ["string", "null"] }
        isActive: { type: boolean }

    PlanningRecord:
      type: object
      properties:
        changeId: { type: integer }
        scope: { type: string }
        implementationPlan: { type: string }
        rollbackPlan: { type: string }
        riskAssessment: { type: string }
        impactedServices: { type: string }
        communicationsPlan: { type: string }
        successCriteria: { type: string }
        signedOff: { type: boolean }
        signedOffAt: { type: ["string", "null"], format: date-time }
        signedOffBy: { type: ["string", "null"] }
      required: [changeId, scope, implementationPlan, rollbackPlan, riskAssessment, impactedServices, communicationsPlan, successCriteria, signedOff]

    UpsertPlanningBody:
      type: object
      properties:
        scope: { type: string }
        implementationPlan: { type: string }
        rollbackPlan: { type: string }
        riskAssessment: { type: string }
        impactedServices: { type: string }
        communicationsPlan: { type: string }
        successCriteria: { type: string }
        signedOff: { type: boolean }
      required: [scope, implementationPlan, rollbackPlan, riskAssessment, impactedServices, communicationsPlan, successCriteria, signedOff]

    TestCase:
      type: object
      properties:
        name: { type: string }
        steps: { type: string }
        expectedResult: { type: string }
        actualResult: { type: string }
        status: { type: string, enum: [pending, passed, failed, blocked] }
      required: [name, steps, expectedResult, actualResult, status]

    TestRecord:
      type: object
      properties:
        changeId: { type: integer }
        testPlan: { type: string }
        environment: { type: string }
        testedBy: { type: ["string", "null"] }
        testedAt: { type: ["string", "null"], format: date-time }
        overallResult: { type: string, enum: [pending, passed, failed] }
        notes: { type: string }
        cases:
          type: array
          items: { $ref: "#/components/schemas/TestCase" }
      required: [changeId, testPlan, environment, overallResult, notes, cases]

    UpsertTestingBody:
      type: object
      properties:
        testPlan: { type: string }
        environment: { type: string }
        overallResult: { type: string, enum: [pending, passed, failed] }
        notes: { type: string }
        cases:
          type: array
          items: { $ref: "#/components/schemas/TestCase" }
      required: [testPlan, environment, overallResult, notes, cases]

    PirRecord:
      type: object
      properties:
        changeId: { type: integer }
        outcome: { type: string, enum: [successful, partial, failed, rolledback] }
        objectivesMet: { type: string }
        issuesEncountered: { type: string }
        lessonsLearned: { type: string }
        followupActions: { type: string }
        completedBy: { type: ["string", "null"] }
        completedAt: { type: ["string", "null"], format: date-time }
      required: [changeId, outcome, objectivesMet, issuesEncountered, lessonsLearned, followupActions]

    UpsertPirBody:
      type: object
      properties:
        outcome: { type: string, enum: [successful, partial, failed, rolledback] }
        objectivesMet: { type: string }
        issuesEncountered: { type: string }
        lessonsLearned: { type: string }
        followupActions: { type: string }
      required: [outcome, objectivesMet, issuesEncountered, lessonsLearned, followupActions]

    Approval:
      type: object
      properties:
        id: { type: integer }
        changeId: { type: integer }
        roleKey: { type: string }
        roleName: { type: string }
        approverId: { type: ["integer", "null"] }
        approverName: { type: ["string", "null"] }
        decision: { type: string, enum: [pending, approved, rejected, abstain] }
        comment: { type: ["string", "null"] }
        decidedAt: { type: ["string", "null"], format: date-time }
        viaDeputy: { type: boolean }
      required: [id, changeId, roleKey, roleName, decision, viaDeputy]

    VoteBody:
      type: object
      properties:
        decision: { type: string, enum: [approved, rejected] }
        comment: { type: ["string", "null"] }
      required: [decision]

    CabMeeting:
      type: object
      properties:
        id: { type: integer }
        title: { type: string }
        kind: { type: string, enum: [cab, ecab] }
        scheduledStart: { type: string, format: date-time }
        scheduledEnd: { type: string, format: date-time }
        location: { type: string }
        agenda: { type: string }
        status: { type: string, enum: [scheduled, in_progress, completed, cancelled] }
        createdAt: { type: string, format: date-time }
      required: [id, title, kind, scheduledStart, scheduledEnd, location, agenda, status, createdAt]

    CabMeetingDetail:
      allOf:
        - $ref: "#/components/schemas/CabMeeting"
        - type: object
          properties:
            members:
              type: array
              items: { $ref: "#/components/schemas/CabMember" }
            changes:
              type: array
              items: { $ref: "#/components/schemas/ChangeRequest" }
            minutes: { type: string }

    CabMember:
      type: object
      properties:
        id: { type: integer }
        userId: { type: integer }
        userName: { type: string }
        userEmail: { type: string }
        roleKey: { type: ["string", "null"] }
        isDeputy: { type: boolean }
      required: [id, userId, userName, userEmail, isDeputy]

    CreateCabMeetingBody:
      type: object
      properties:
        title: { type: string }
        kind: { type: string, enum: [cab, ecab] }
        scheduledStart: { type: string, format: date-time }
        scheduledEnd: { type: string, format: date-time }
        location: { type: string }
        agenda: { type: string }
        timeZone:
          type: string
          description: Creator's IANA timezone; recurring occurrences keep the same wall-clock time in this zone across DST changes.
        recurring: { type: boolean }
        recurrenceIntervalWeeks: { type: integer }
        recurrenceUntil: { type: string, format: date }
        memberUserIds:
          type: array
          items: { type: integer }
        changeIds:
          type: array
          items: { type: integer }
      required: [title, kind, scheduledStart, scheduledEnd, location, agenda, memberUserIds, changeIds]

    UpdateCabMeetingBody:
      type: object
      properties:
        title: { type: string }
        scheduledStart: { type: string, format: date-time }
        scheduledEnd: { type: string, format: date-time }
        location: { type: string }
        agenda: { type: string }
        status: { type: string, enum: [scheduled, in_progress, completed, cancelled] }
        memberUserIds:
          type: array
          items: { type: integer }
        changeIds:
          type: array
          items: { type: integer }
        minutes: { type: string }

    Comment:
      type: object
      properties:
        id: { type: integer }
        changeId: { type: integer }
        authorId: { type: integer }
        authorName: { type: string }
        body: { type: string }
        createdAt: { type: string, format: date-time }
      required: [id, changeId, authorId, authorName, body, createdAt]

    CreateCommentBody:
      type: object
      properties:
        body: { type: string }
      required: [body]

    DashboardSummary:
      type: object
      properties:
        totalChanges: { type: integer }
        openChanges: { type: integer }
        awaitingApproval: { type: integer }
        scheduledThisWeek: { type: integer }
        emergencyOpen: { type: integer }
        successRate: { type: number }
        byStatus:
          type: array
          items:
            type: object
            properties:
              status: { type: string }
              count: { type: integer }
            required: [status, count]
        byTrack:
          type: array
          items:
            type: object
            properties:
              track: { type: string }
              count: { type: integer }
            required: [track, count]
        byRisk:
          type: array
          items:
            type: object
            properties:
              risk: { type: string }
              count: { type: integer }
            required: [risk, count]
      required: [totalChanges, openChanges, awaitingApproval, scheduledThisWeek, emergencyOpen, successRate, byStatus, byTrack, byRisk]

    ActivityItem:
      type: object
      properties:
        id: { type: integer }
        timestamp: { type: string, format: date-time }
        actorName: { type: string }
        action: { type: string }
        entityType: { type: string }
        entityId: { type: ["integer", "null"] }
        summary: { type: string }
      required: [id, timestamp, actorName, action, entityType, summary]

    TaskItem:
      type: object
      properties:
        id: { type: string }
        kind: { type: string, enum: [approval, review, planning, testing, pir, cab] }
        title: { type: string }
        changeId: { type: ["integer", "null"] }
        changeRef: { type: ["string", "null"] }
        dueAt: { type: ["string", "null"], format: date-time }
      required: [id, kind, title]

    SmtpSettings:
      type: object
      properties:
        host: { type: string }
        port: { type: integer }
        secure: { type: boolean }
        username: { type: string }
        password: { type: ["string", "null"] }
        fromAddress: { type: string }
        fromName: { type: string }
        enabled: { type: boolean }
      required: [host, port, secure, username, fromAddress, fromName, enabled]

    TestEmailBody:
      type: object
      properties:
        to: { type: string }
      required: [to]

    TestResult:
      type: object
      properties:
        success: { type: boolean }
        message: { type: string }
      required: [success, message]

    LdapSettings:
      type: object
      properties:
        enabled: { type: boolean }
        url: { type: string }
        bindDn: { type: string }
        bindPassword: { type: ["string", "null"] }
        baseDn: { type: string }
        userFilter: { type: string }
        usernameAttr: { type: string }
        emailAttr: { type: string }
        nameAttr: { type: string }
        tls: { type: boolean }
      required: [enabled, url, bindDn, baseDn, userFilter, usernameAttr, emailAttr, nameAttr, tls]

    TestLdapBody:
      type: object
      properties:
        username: { type: string }
        password: { type: string }
      required: [username, password]

    LdapTestResult:
      type: object
      required: [success, stage, message]
      properties:
        success: { type: boolean, description: "True if the user-bind succeeded end-to-end." }
        stage:
          type: string
          enum: [config, connect, service-bind, search, user-bind, ok]
          description: "Phase the bind reached. On failure this is the phase that broke."
        message: { type: string, description: "Human-readable summary suitable for showing to the admin." }
        code:    { type: string, description: "LDAP result-code name (e.g. InvalidCredentialsError) or socket error code (e.g. ECONNREFUSED). Optional." }
        details: { type: string, description: "Server-side diagnostic message from the directory, when one was returned. Optional." }
        userDn:  { type: string, description: "The full DN that was resolved and bound. Populated only on success." }

    SslSettings:
      type: object
      properties:
        hasCertificate: { type: boolean }
        commonName: { type: ["string", "null"] }
        issuer: { type: ["string", "null"] }
        notBefore: { type: ["string", "null"], format: date-time }
        notAfter: { type: ["string", "null"], format: date-time }
        sanList:
          type: array
          items: { type: string }
        forceHttps: { type: boolean }
        hstsEnabled: { type: boolean }
      required: [hasCertificate, sanList, forceHttps, hstsEnabled]

    UpdateSslBody:
      type: object
      properties:
        certificatePem: { type: ["string", "null"] }
        privateKeyPem: { type: ["string", "null"] }
        chainPem: { type: ["string", "null"] }
        forceHttps: { type: boolean }
        hstsEnabled: { type: boolean }

    AuditEntry:
      type: object
      properties:
        id: { type: integer }
        timestamp: { type: string, format: date-time }
        actorId: { type: ["integer", "null"] }
        actorName: { type: string }
        action: { type: string }
        entityType: { type: string }
        entityId: { type: ["integer", "null"] }
        summary: { type: string }
        ipAddress: { type: string }
        userAgent: { type: string }
        before: {}
        after: {}
      required: [id, timestamp, actorName, action, entityType, summary, ipAddress, userAgent]
